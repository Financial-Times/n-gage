machine:
  node:
    version: 6.0.0
deployment:
  release:
    tag: /v.*/
    owner: Financial-Times
    commands:
      - npm version --no-git-tag-version ${CIRCLE_TAG}
      - npm publish --access public
